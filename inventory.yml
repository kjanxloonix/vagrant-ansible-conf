devservers:
  hosts:
    dev.vagrant.local:
      ansible_user: vagrant
      ansible_ssh_private_key_file: .vagrant/machines/dev/virtualbox/private_key
tomcatservers:
  hosts:
    tomcat.vagrant.local:
      ansible_user: vagrant
      ansible_ssh_private_key_file: .vagrant/machines/tomcat/virtualbox/private_key
jenkinsservers:
  hosts:
    jenkins.vagrant.local:
      ansible_user: vagrant
      ansible_ssh_private_key_file: .vagrant/machines/jenkins/virtualbox/private_key
dbservers:
  hosts:
    db.vagrant.local:
      ansible_user: vagrant
      ansible_ssh_private_key_file: .vagrant/machines/db/virtualbox/private_key
mailservers:
  hosts:
    mx.vagrant.local:
      ansible_user: vagrant
      ansible_ssh_private_key_file: .vagrant/machines/mail/virtualbox/private_key
